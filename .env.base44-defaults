# Base44 local defaults — placeholders so the app boots without credentials.
# Real secrets are delivered via /run/base44/app.env (loaded last, overrides these).
GEMINI_API_KEY=""
APP_URL="http://localhost:3000"
